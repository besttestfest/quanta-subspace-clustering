# QDG Pipeline Requirements
# Install: pip install -r requirements.txt --break-system-packages
#
# torch is included with no version pin so pip picks the right wheel for
# the host (CPU-only or one of several CUDA builds). On UCloud GPU images
# pip's wheel-resolver will select a CUDA-enabled build automatically.
# If you need a specific CUDA build, install torch separately first:
#   pip install torch==2.2.0+cu121 --extra-index-url https://download.pytorch.org/whl/cu121

# Core scientific
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4

# Deep learning
torch>=2.0
transformers>=4.38
datasets>=2.18

# Visualization
matplotlib>=3.9
tqdm>=4.66

# Parallelism (used by 02_clustering.py inner Lasso loop)
joblib>=1.3

# Arrow / Parquet (used by setup/compute_losses.py)
pyarrow>=14.0
